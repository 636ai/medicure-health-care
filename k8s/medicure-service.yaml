apiVersion: v1
kind: Service
metadata:
  name: medicure-service
spec:
  type: LoadBalancer
  selector:
    app: medicure
  ports:
    - protocol: TCP
      port: 80       # External port
      targetPort: 8080  # Container port
